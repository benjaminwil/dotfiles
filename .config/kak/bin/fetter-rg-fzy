#!/bin/sh

. fetter_init-check

selected_file="$(rg \
    "$KAKOUNE_CURRENT_WORKING_DIRECTORY" --files | \
        fzy --lines 99)"

printf "evaluate-commands -try-client $KAKOUNE_CLIENT -verbatim -- edit! -existing '$selected_file'" | \
    kak -p $KAKOUNE_SESSION
