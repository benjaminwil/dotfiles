#!/usr/bin/ruby

candidates = ARGV.rotate(1)
existing_sessions = %x{ kak -l }.split

unless (ARGV.size > 0)
  puts "usage:"
  puts "    kak-sesh <current-session-name> [, candidates]"
end

puts candidates.detect { |candidate| !existing_sessions.include? candidate }
