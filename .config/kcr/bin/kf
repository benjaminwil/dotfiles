#!/bin/sh

. _kinit

set -x
echo "$KAKOUNE_CURRENT_WORKING_DIRECTORY"

selected_file="$(rg \
    "$KAKOUNE_CURRENT_WORKING_DIRECTORY" --files | \
        fzy --lines 99)"

command kcr edit "$selected_file"
